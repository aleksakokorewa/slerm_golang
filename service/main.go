package service

import "fmt"

const (
	PassStatus = "pass"
	FailStatus = "fail"
)

func main() {
	fmt.Println("Тут будет выведен идентификатор")
	//check := GenerateCheck()
	//for _, item := range check {
	//	if item.status == PassStatus {
	//		fmt.Println(item.Id)
	//	}
}
